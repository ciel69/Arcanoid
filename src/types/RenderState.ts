export interface RenderState {
  frame: number,
  frameCount: number,
  timeStat: string,
  fps: number,
  fpsInterval: number,
  startTime: number,
  sinceStart: number,
  now: number,
  then: number,
  elapsed: number,
  currentFps: number,
}